# Direct dependencies for the project.
# Use `pip install -r requirements.txt` to install.
# To generate a full list of pinned dependencies (including transitive ones),
# you can use `pip freeze > requirements.lock.txt` after installing from this file.

google-generativeai==0.8.5
numpy==2.2.6
ortools==9.12.4544
pandas==2.2.3
python-dateutil==2.9.0.post0
streamlit==1.45.1
